# landing

Public marketing site — where a visitor reads the pitch and signs up. Not built
yet.

Will be Vite + React like the rest. When it ships, add `vite-react-ssg` to
prerender routes to static HTML — social crawlers (Discord, X, LinkedIn, Slack)
don't execute JS, so without prerendering a shared link has no preview card.
